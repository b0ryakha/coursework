<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://getbootstrap.com/docs/5.0/dist/css/bootstrap.min.css">
    <title>ТехноШоп</title>
  </head>
  <body>
    <%- include("partials/header") %>

    <div class="container">
      <h1 class="mb-4 mt-3">Каталог</h1>
      <div class="row">
        <% data.forEach(item => { %>
          <div class="col-2 card m-auto mb-3 ms-0" style="width: 19rem;">
            <img src="/images/noimg.webp" class="mt-2" alt="..." style="border-radius:3px;">
            <div class="card-body">
              <h5 class="card-title"><%= item.title %></h5>
              <p class="card-text"><%= item.cost %> руб.</p>
              <a href="/order" class="btn btn-dark">Заказать</a>
            </div>
          </div>
        <% }); %>
      </div>
    </div>
  </body>
</html>