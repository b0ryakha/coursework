<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Data Page</title>
</head>
<body>
  <h1>Data from Repository</h1>
  <ul>
    <% data.forEach(item => { %>
      <li><%= item.name %> (ID: <%= item.id %>)</li>
    <% }); %>
  </ul>
</body>
</html>